--- a/src/common/cp_files.F
+++ b/src/common/cp_files.F
@@ -491,11 +491,12 @@ CONTAINS
     CHARACTER(LEN=*), INTENT(IN)             :: file_name
     CHARACTER(LEN=default_path_length)       :: real_file_name
 
-    CHARACTER(LEN=default_path_length)       :: candidates(3), data_path
+    CHARACTER(LEN=default_path_length)       :: candidates(4), data_path
     INTEGER                                  :: i, stat
     LOGICAL                                  :: exists
 
     candidates(:) = file_name
+    candidates(4) = "<data-dir>" // TRIM(file_name)
 
     CALL GET_ENVIRONMENT_VARIABLE("CP2K_DATA_DIR", data_path, status=stat)
     IF(stat == 0) &
-- 
1.7.1

